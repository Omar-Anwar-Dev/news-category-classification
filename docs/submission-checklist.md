# Submission Checklist — M2 Final Delivery

This is the punch list for **S3-T11**. The pre-flight portion (Sections 1-3) was verified at end of Sprint 3 day 1 and all boxes are ticked. The remaining sections (4-7) are the steps to take **after** the team's two rehearsals (S3-T8 / S3-T10) and the bug-fix sweep (S3-T9), to actually ship `v1.0`.

The order matters: do not jump to tagging until everything above the **`tag v1.0`** line is verified green on the same commit.

---

## 1. Deliverables present (all ticked at pre-flight)

Spec requires 4 + 2 artefacts. All present in the Sprint-3 worktree on 2026-05-09:

| # | Deliverable                  | Path                                     | Size       | Notes |
| - | ---------------------------- | ---------------------------------------- | ---------- | ----- |
| 1 | Structured Jupyter notebook  | `notebooks/00_main.ipynb`                | 66 KB      | 39 cells, sections 1-15 (S3-T1) |
| 1b | EDA notebook (Part 1 + 2)   | `notebooks/01_eda.ipynb`                 | 17 KB      | sibling of 00_main |
| 2 | Gradio app                   | inside `00_main.ipynb` §14               | —          | launches on `Run all` |
| 3 | Slide deck                   | `docs/slides.pptx`                       | 416 KB     | 15 slides, embedded confusion-matrix on §8 (S3-T4 / S3-T7) |
| 4 | Written report               | `docs/report.md` + `docs/report.docx`    | 13 KB + 401 KB | ~5 A4 pages, plain language (S3-T5 / S3-T7) |
| 5 | README                       | `README.md`                              | 9 KB       | clone → Gradio link in <15 min (S3-T2) |
| 6 | Pinned dependencies          | `requirements.txt`                       | 1.8 KB     | M2-frozen (S3-T3) |

Plus internal docs (PRD, architecture, decisions, implementation plan, progress, demo runbook) under `docs/` — useful for the grader, not strictly required by spec.

## 2. Artefacts referenced by the deliverables

These files must exist (and be tracked by git) for the deliverables to render correctly. All present and tracked after the `.gitignore` adjustment:

- [x] `reports/model_comparison.csv` — 7 rows (header + 7 models), real Sprint-2 metrics
- [x] `reports/confusion/roberta_finetuned_hires.png` — embedded in slide 8 + report.docx
- [x] `reports/confusion/{adaboost,decision_tree,knn,linearsvc,logreg,random_forest,roberta_finetuned}.png` — one PNG per model, referenced by the comparison narrative
- [x] `src/preprocessing.py` — imported by the notebook §2 + by `tests/test_preprocessing.py`
- [x] `tests/test_preprocessing.py` + `tests/test_smoke.py` — the 8 CI tests
- [x] `.github/workflows/ci.yml` — runs ruff + pytest on PR + push

## 3. Quality gates (all green at pre-flight)

- [x] **Ruff lint** — `python -m ruff check .` → *All checks passed* (5 Python files)
- [x] **Ruff format** — `python -m ruff format --check .` → *5 files already formatted*
- [x] **Pytest** — `python -m pytest -v` → *8 passed in 9.07 s*
- [x] **Notebook validity** — `nbformat` v4.5, 39 cells, sections numbered 1..15 contiguously
- [x] **PPTX layout** — every shape across the 15 slides fits inside 13.33×7.5 in canvas
- [x] **DOCX integrity** — `word/media/image1.png` (399,032 bytes) embedded
- [x] **No AI-assistant brand mentions** — repo-wide grep returns zero matches across `*.{md,py,ipynb,txt,toml,yml,yaml}` for any AI-assistant brand name or attribution phrase ("generated by AI" / "written by AI" / etc.). The only literal mention of any AI brand is *OpenAI GPT-4o-mini* in `docs/decisions.md` ADR-001, where it appears as the rejected alternative in an LLM-choice ADR — a legitimate product reference, not an attribution.

## 4. Steps after the two rehearsals — **do not start before S3-T9 finishes**

### 4a. Resolve every S0/S1 row in `docs/rehearsal-2-notes.md`

S0 = blocking, S1 = should-fix. Both must be cleared. S2 (nice-to-have) can be deferred to post-MVP.

### 4b. Final notebook check on a fresh Colab T4

- [ ] Open the live Colab notebook from `README.md` Quick-Start §A.
- [ ] Runtime → Disconnect and delete runtime.
- [ ] Re-attach a fresh T4 GPU runtime.
- [ ] Add the three Colab Secrets (Kaggle username, Kaggle key, Groq API key).
- [ ] Runtime → Run all.
- [ ] Confirm: §1 bootstrap green, §2-§14 run end-to-end, §14 prints a public Gradio link, §15 prints the final summary block.
- [ ] Walk through the three demo examples (Apple iPhone, Lakers/Celtics, climate bill) on the live link.

### 4c. Confirm a teammate can clone-and-run

- [ ] A teammate (not the coordinator) clones the public repo on a different machine.
- [ ] Follows `README.md` Quick-Start §A end-to-end.
- [ ] Reaches a working Gradio share link.
- [ ] Reports any README friction back as a final S3-T9 patch.

## 5. Stage and commit the Sprint-3 work

A single Sprint-3 sign-off commit that bundles all the polish work. **Do not skip pre-commit hooks.**

Files to stage:

```
.gitignore                                  (modified — confusion-PNG negation)
README.md                                    (modified — full final-delivery rewrite)
docs/progress.md                             (modified — Sprint-3 log)
docs/demo-runbook.md                         (new)
docs/report.md                               (new)
docs/report.docx                             (new)
docs/slides.pptx                             (new)
docs/submission-checklist.md                 (new — this file)
notebooks/00_main.ipynb                      (modified — §1..15 polish + §15 summary)
reports/model_comparison.csv                 (modified — overwrote with real 7-row Sprint-2 data)
reports/confusion/adaboost.png               (new — Sprint-2 artefact, now tracked)
reports/confusion/decision_tree.png          (new)
reports/confusion/knn.png                    (new)
reports/confusion/linearsvc.png              (new)
reports/confusion/logreg.png                 (new)
reports/confusion/random_forest.png          (new)
reports/confusion/roberta_finetuned.png      (new)
reports/confusion/roberta_finetuned_hires.png (new — referenced by report.md and slide 8)
requirements.txt                             (modified — frozen, header rewritten for M2)
```

Suggested commit message (one commit, three lines):

```
S3 sign-off: M2 polished delivery — notebook §1..15, README final, slides, report

Sprint 3 deliverables: notebook polish + final summary cell; final-delivery README
with three Colab Secrets and live-Colab quick-start; .pptx slide deck (15 slides);
plain-language .md + .docx report (~5 pages) with embedded confusion matrix; demo
runbook + rehearsal templates; .gitignore now tracks the 8 confusion-matrix PNGs.
Quality gates: ruff lint + format clean, 8/8 pytest green, repo-wide grep clean.
```

## 6. Tag and push v1.0

Only after Section 4 is complete and Section 5 is committed cleanly:

- [ ] `git tag -a v1.0 -m "M2 — Polished Delivery"`
- [ ] `git push origin main`
- [ ] `git push origin v1.0`
- [ ] Verify the tag is visible on GitHub at `https://github.com/Omar-Anwar-Dev/news-category-classification/releases/tag/v1.0`

## 7. Build the submission ZIP

The ZIP is the second submission channel alongside the GitHub link. Build it from a clean checkout, **not** from this worktree (which contains harness state).

- [ ] `git clone --depth=1 --branch=v1.0 https://github.com/Omar-Anwar-Dev/news-category-classification.git news-classification-v1.0`
- [ ] Strip any local-only ignored content if present (none should be there from a fresh clone).
- [ ] Zip with the directory at the top level: `cd news-classification-v1.0 && zip -r ../news-classification-v1.0.zip .`
- [ ] Verify the ZIP contents:
  - [ ] `notebooks/00_main.ipynb` and `notebooks/01_eda.ipynb` parse as valid `.ipynb`
  - [ ] `docs/slides.pptx` opens cleanly in PowerPoint
  - [ ] `docs/report.docx` opens cleanly in Word with the confusion-matrix image rendered
  - [ ] `README.md` Quick-Start section renders correctly when previewed on GitHub
  - [ ] No `__pycache__/`, `.venv/`, harness session-state directories, or `kaggle.json` smuggled in
- [ ] Final check on the ZIP: file size sanity (expect ~600 KB — slides + docx dominate; data, models, embeddings are gitignored and will not be in the ZIP, which is intentional — they live on the public Drive folder linked from the README)

## 8. The two URLs to hand in

When submitting:

- **GitHub repo (tagged):** `https://github.com/Omar-Anwar-Dev/news-category-classification/tree/v1.0`
- **ZIP file:** `news-classification-v1.0.zip` (built per Section 7)
- **Public Drive folder** with all M1 artefacts (model weights, embeddings, comparison CSV, confusion PNGs) — linked from `README.md` Quick-Start §A.

The grader can use either entry point; both reach the same `v1.0` state of the codebase.
